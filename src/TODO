TODO
For compiler, Always return new ModuleDef. Do not mutate it

Error handling
Maybe Error and warning system instead of just throwing errors?

in toFile, Change Type.Type to Type.Kind


Type system overhaul.
///////////////////////////////////////


TODO Type::sel. What should it do for a bad select?

//Adding Metadata
Module:
  Data ->
    Verilog
    Simulate
    Driver
    OtherData
  Circuit stuff

Primitives TODO:
-Add generators for constants
-And all other normal primitives

Possibly should check for cases of modules instantiated in its own module. We dont want module "recursion"
